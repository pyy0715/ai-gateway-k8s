# kind 클러스터 설정 - Envoy AI Gateway 실습용
# 포트 80 -> 8081 매핑으로 로컬에서 접근 가능
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 80
    hostPort: 8081
    protocol: TCP
  - containerPort: 443
    hostPort: 8444
    protocol: TCP
